- Add gamepad API for inputs
- Add modern javascript out file (legacy & polyfills output)

TODAY:

- --Add "from fusions" to.--
- --Fix search bar--
- --HTML metadata per card? (Better sitemap?)--
- --Add equip card modifications--
- --Add localstorage for speed button--
- --Fix names with # in the URL (winged dragon # 2 and # 1)--
- --Fix predicted fusion algorithm--
- --Fix equip fusions prediction total stats--
- --Add button for toggle predict--

- Add spreadsheet of "Fusions FM" (or a guide that includes it)
- Add tutorial and guide section (SEO improvement also)
- Add VDH instead of VH (Try it on tablet & phone)

* Check all memos, effects that use "hand" because when it is focus the state will change

+ Add home page, continue improving the SEO.
+ Add SSG.